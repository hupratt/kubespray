apiVersion: v1
kind: Service
metadata:
  name: {{ include "vanity.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "vanity.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080