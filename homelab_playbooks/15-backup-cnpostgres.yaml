---
- name: Provision Postgres backup CronJob
  hosts: localhost
  gather_facts: false
  vars:
    kubeconfig_path: ~/.kube/config
    vault_kv_path: restic

  tasks:
    - name: Deploy ExternalSecret 
      kubernetes.core.k8s:
        state: present
        definition:
          apiVersion: external-secrets.io/v1
          kind: ExternalSecret
          metadata:
            name: aws-credentials
            namespace: cn-postgres
          spec:
            refreshInterval: 1h
            secretStoreRef:
              name: vault-backend
              kind: ClusterSecretStore
            target:
              name: aws-credentials
              creationPolicy: Owner
            data:
              - secretKey: AWS_ACCESS_KEY_ID
                remoteRef:
                  key: "{{ vault_kv_path }}"
                  property: AWS_ACCESS_KEY_ID
              - secretKey: AWS_SECRET_ACCESS_KEY
                remoteRef:
                  key: "{{ vault_kv_path }}"
                  property: AWS_SECRET_ACCESS_KEY

    - name: Deploy pgdump CronJob
      kubernetes.core.k8s:
        state: present
        definition:
          apiVersion: batch/v1
          kind: CronJob
          metadata:
            name: pgdump-backup
            namespace: cn-postgres
          spec:
            schedule: "0 0 31 2 *"
            successfulJobsHistoryLimit: 3
            failedJobsHistoryLimit: 3
            jobTemplate:
              spec:
                backoffLimit: 0
                template:
                  spec:
                    restartPolicy: Never
                    securityContext:
                      runAsUser: 70
                      runAsGroup: 70
                      runAsNonRoot: true
                    imagePullSecrets:
                      - name: docker-registry-secret
                    nodeSelector:
                      kubernetes.io/hostname: coreos-cp-1
                    containers:
                      - name: pgdump
                        image: registry.thekor.eu/docker/k8s-fedora-backup/postgres-backup:17-alpine-awscli
                        command:
                          - /bin/bash
                          - -c
                          - /home/postgres/backupoutput/sql_postgres.sh
                        env:
                          - name: AWS_ACCESS_KEY_ID
                            valueFrom:
                              secretKeyRef:
                                name: aws-credentials
                                key: AWS_ACCESS_KEY_ID
                          - name: AWS_SECRET_ACCESS_KEY
                            valueFrom:
                              secretKeyRef:
                                name: aws-credentials
                                key: AWS_SECRET_ACCESS_KEY
                          - name: PGHOST
                            value: "{{ POSTGRES_HOST }}"
                          - name: PGUSER
                            value: "{{ POSTGRES_BACKUP_USER }}"
                          - name: PGPASSWORD
                            value: "{{ POSTGRES_BACKUP_PASSWORD }}"
                          - name: PG_TARGETS
                            value: "grafana linkwarden netbox authentik makita paperless harbor-core harbor-notary-server harbor-notary-signer harbor-trivy booking patchmon youtubeclone backupmatrix"
                        volumeMounts:
                          - name: backupoutput
                            mountPath: /home/postgres/backupoutput
                        livenessProbe:
                          exec:
                            command: ["true"]
                          initialDelaySeconds: 5
                          periodSeconds: 30
                        readinessProbe:
                          exec:
                            command: ["true"]
                          initialDelaySeconds: 5
                          periodSeconds: 30
                    volumes:
                      - name: backupoutput
                        hostPath:
                          path: /mnt/backupoutput
                          type: DirectoryOrCreate

## need to edit node01
# setfacl -m mask::rwx /mnt/backupoutput/
# setfacl -m mask::rwx /mnt/backupoutput/*.sh