---
- name: Configure slow-services node storage
  hosts: localhost
  gather_facts: false
  vars:
    slow_node: coreos-wk-4
    storage_base: /var/mnt/slow-storage
    storage_class: slow-local
    pvs:
      # technitium/technitium-technitium-dnsserver-config
      - name: slow-pv-01
        size: 50Gi
      # immich/immich-pg-1
      - name: slow-pv-03
        size: 100Gi
      - name: immich-restore-temp
        size: 3000Gi
      - name: volsync-restore-immich-cache
        size: 1Gi
      - name: volsync-src-immich-library-backup-cache
        size: 1Gi

  tasks:

    - name: Create StorageClass (no-provisioner)
      kubernetes.core.k8s:
        state: present
        definition:
          apiVersion: storage.k8s.io/v1
          kind: StorageClass
          metadata:
            name: "{{ storage_class }}"
          provisioner: kubernetes.io/no-provisioner
          volumeBindingMode: WaitForFirstConsumer
          reclaimPolicy: Retain

    - name: Create PersistentVolumes
      kubernetes.core.k8s:
        state: present
        definition:
          apiVersion: v1
          kind: PersistentVolume
          metadata:
            name: "{{ item.name }}"
          spec:
            capacity:
              storage: "{{ item.size }}"
            accessModes:
              - ReadWriteOnce
            persistentVolumeReclaimPolicy: Retain
            storageClassName: "{{ storage_class }}"
            local:
              path: "{{ storage_base }}/{{ item.name }}"
            nodeAffinity:
              required:
                nodeSelectorTerms:
                  - matchExpressions:
                      - key: node-role
                        operator: In
                        values:
                          - slow-services
      loop: "{{ pvs }}"

    - name: Ensure storage directories exist on slow node
      ansible.builtin.file:
        path: "{{ storage_base }}/{{ item.name }}"
        state: directory
        mode: "0750"
      loop: "{{ pvs }}"
      delegate_to: "{{ slow_node }}"
      become: true

    - name: Create PersistentVolumes
      kubernetes.core.k8s:
        state: present
        definition:
          apiVersion: v1
          kind: PersistentVolume
          metadata:
            name: immich-library
          spec:
            capacity:
              storage: 3000Gi
            accessModes:
              - ReadWriteOnce
            persistentVolumeReclaimPolicy: Retain
            storageClassName: "{{ storage_class }}"
            local:
              path: "/var/mnt/immich/immich/library"
            nodeAffinity:
              required:
                nodeSelectorTerms:
                  - matchExpressions:
                      - key: node-role
                        operator: In
                        values:
                          - slow-services

    - name: Create PersistentVolume youtubeclone-static
      kubernetes.core.k8s:
        state: present
        definition:
          apiVersion: v1
          kind: PersistentVolume
          metadata:
            name: youtubeclone-static
          spec:
            capacity:
              storage: 3000Gi
            accessModes:
              - ReadWriteOnce
            persistentVolumeReclaimPolicy: Retain
            storageClassName: "{{ storage_class }}"
            local:
              path: "/var/mnt/youtube/youtubeclone-dockerized/static"
            nodeAffinity:
              required:
                nodeSelectorTerms:
                  - matchExpressions:
                      - key: node-role
                        operator: In
                        values:
                          - slow-services

    - name: Create PersistentVolume youtubeclone-uploads
      kubernetes.core.k8s:
        state: present
        definition:
          apiVersion: v1
          kind: PersistentVolume
          metadata:
            name: youtubeclone-uploads
          spec:
            capacity:
              storage: 100Mi
            accessModes:
              - ReadWriteOnce
            persistentVolumeReclaimPolicy: Retain
            storageClassName: "{{ storage_class }}"
            local:
              path: "/var/mnt/youtube/youtubeclone-dockerized/youtubeclone-backend/uploads"
            nodeAffinity:
              required:
                nodeSelectorTerms:
                  - matchExpressions:
                      - key: node-role
                        operator: In
                        values:
                          - slow-services

    - name: Create PersistentVolume youtubeclone-vids
      kubernetes.core.k8s:
        state: present
        definition:
          apiVersion: v1
          kind: PersistentVolume
          metadata:
            name: youtubeclone-vids
          spec:
            capacity:
              storage: 100Gi
            accessModes:
              - ReadWriteOnce
            persistentVolumeReclaimPolicy: Retain
            storageClassName: "{{ storage_class }}"
            local:
              path: "/var/mnt/youtube/youtubeclone-dockerized/vids"
            nodeAffinity:
              required:
                nodeSelectorTerms:
                  - matchExpressions:
                      - key: node-role
                        operator: In
                        values:
                          - slow-services

    - name: Create PersistentVolume scriberr-data-pv
      kubernetes.core.k8s:
        state: present
        definition:
          apiVersion: v1
          kind: PersistentVolume
          metadata:
            name: scriberr-data-pv
          spec:
            capacity:
              storage: 10Gi
            accessModes:
              - ReadWriteOnce
            persistentVolumeReclaimPolicy: Retain
            storageClassName: "{{ storage_class }}"
            local:
              path: "/var/mnt/youtube/scriberr/scriberr_data"
            nodeAffinity:
              required:
                nodeSelectorTerms:
                  - matchExpressions:
                      - key: node-role
                        operator: In
                        values:
                          - slow-services

    - name: Create PersistentVolume scriberr-whisperx-env-pv
      kubernetes.core.k8s:
        state: present
        definition:
          apiVersion: v1
          kind: PersistentVolume
          metadata:
            name: scriberr-whisperx-env-pv
          spec:
            capacity:
              storage: 100Gi
            accessModes:
              - ReadWriteOnce
            persistentVolumeReclaimPolicy: Retain
            storageClassName: "{{ storage_class }}"
            local:
              path: "/var/mnt/youtube/scriberr/env_data"
            nodeAffinity:
              required:
                nodeSelectorTerms:
                  - matchExpressions:
                      - key: node-role
                        operator: In
                        values:
                          - slow-services