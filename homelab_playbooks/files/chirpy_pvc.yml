apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: rook-cephfs-chirpy
provisioner: rook-ceph.cephfs.csi.ceph.com
parameters:
  clusterID: rook-ceph
  fsName: myfs
  pool: myfs-data0
  subvolumeGroup: csi
  csi.storage.k8s.io/provisioner-secret-name: rook-csi-cephfs-provisioner
  csi.storage.k8s.io/provisioner-secret-namespace: rook-ceph
  csi.storage.k8s.io/node-stage-secret-name: rook-csi-cephfs-node
  csi.storage.k8s.io/node-stage-secret-namespace: rook-ceph
  monitors: ceph-mon.rook-ceph.svc.cluster.local
  mounter: fuse  # Use FUSE instead of kernel client
mountOptions:
  - debug
reclaimPolicy: Delete
allowVolumeExpansion: true

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: chirpy-pvc
  namespace: chirpy
spec:
  storageClassName: rook-cephfs-chirpy
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Mi

# ceph fs subvolume ls myfs csi
# [
#     {
#         "name": "csi-vol-8fc18bf3-beab-4f3b-95a7-d9784c5e55bb"
#     }
# ]

# kubectl -n rook-ceph rollout restart deployment/csi-cephfsplugin-provisioner
# kubectl -n rook-ceph rollout restart deployment/csi-rbdplugin-provisioner
