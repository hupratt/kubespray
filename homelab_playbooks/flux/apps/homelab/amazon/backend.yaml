---
# Migrated from 07-amazon.yaml
#   task: "Deploy Amazon clone stack to Kubernetes"
#         (amazon-backend Deployment + Service + Ingress)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: amazon-backend
  namespace: amazon-clone
spec:
  replicas: 1
  selector:
    matchLabels:
      app: amazon-backend
  template:
    metadata:
      labels:
        app: amazon-backend
    spec:
      imagePullSecrets:
        - name: docker-registry-secret
      containers:
        - name: amazon-backend
          image: registry.thekor.eu/docker/amazon/back:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8966
          env:
            - name: PORT
              value: "8966"
            - name: MONGO_URL
              valueFrom:
                secretKeyRef:
                  name: amazon-database-url
                  key: amazon-database-url
          securityContext:
            runAsUser: 1001
            runAsNonRoot: true
            capabilities:
              drop: ["ALL"]
          livenessProbe:
            httpGet:
              path: /health
              port: 8966
            initialDelaySeconds: 30
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health
              port: 8966
            initialDelaySeconds: 10
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: amazon-backend
  namespace: amazon-clone
spec:
  selector:
    app: amazon-backend
  ports:
    - port: 8966
      targetPort: 8966
  type: ClusterIP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: amazon-backend-ingress
  namespace: amazon-clone
  annotations:
    haproxy.org/check: "disabled"
    haproxy.org/backend-config-snippet: |
      no option httpchk
spec:
  ingressClassName: haproxy
  tls:
    - hosts:
        - retroapi.thekor.eu
      secretName: wildcard-thekor
  rules:
    - host: retroapi.thekor.eu
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: amazon-backend
                port:
                  number: 8966
