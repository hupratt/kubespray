---
# Migrated from deploy-backup.sh (the imperative companion to 13-backupuser.yaml).
# `kubectl create sa backup -n backup` is the GitOps-able part; the SA token
# Secret generation + kubeconfig assembly/scp to the external pull-backup host
# stay imperative (see BLOCKERS / flux README). The RoleBindings in this dir bind
# this ServiceAccount.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: backup
  namespace: backup
