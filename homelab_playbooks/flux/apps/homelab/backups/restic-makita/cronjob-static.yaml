---
# Migrated verbatim from the "Deploy restic backup CronJob for static pvc" task
# in 42-restic-makita.yaml. Backs up the makita-static PVC.
# NOTE: the source playbook places `env` at the pod-spec level (not inside a
# container); moved into each container where it belongs (invalid at pod level).
apiVersion: batch/v1
kind: CronJob
metadata:
  name: makita-static
  namespace: makita
spec:
  schedule: "0 0 31 2 *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 1
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            runAsGroup: 1000
            fsGroup: 1000
            seccompProfile:
              type: RuntimeDefault
          initContainers:
            - name: restic-init
              image: instrumentisto/restic:0.18.0
              securityContext:
                runAsNonRoot: true
                runAsUser: 1000
                allowPrivilegeEscalation: false
                capabilities:
                  drop: ["ALL"]
              command:
                - /bin/sh
                - -c
                - |
                  set -e
                  restic snapshots >/dev/null 2>&1 || restic init
              env:
                - name: RESTIC_CACHE_DIR
                  value: /tmp/restic-cache
              envFrom:
                - secretRef:
                    name: makita-secrets
          containers:
            - name: restic
              image: instrumentisto/restic:0.18.0
              securityContext:
                runAsNonRoot: true
                runAsUser: 1000
                allowPrivilegeEscalation: false
                readOnlyRootFilesystem: false
                capabilities:
                  drop: ["ALL"]
              command:
                - /bin/sh
                - -c
                - |
                  set -e
                  restic backup /data --host makita-static
                  restic forget \
                    --keep-daily 7 \
                    --keep-weekly 4 \
                    --keep-monthly 6 \
                    --prune
              env:
                - name: RESTIC_CACHE_DIR
                  value: /tmp/restic-cache
              envFrom:
                - secretRef:
                    name: makita-secrets
              volumeMounts:
                - name: data
                  mountPath: /data
                  readOnly: true
          volumes:
            - name: data
              persistentVolumeClaim:
                claimName: makita-static
