---
# Migrated verbatim from 37-ilo-exporter.yaml, task "Deploy iLO exporter manifests"
# (Service). port -> 9545
apiVersion: v1
kind: Service
metadata:
  name: ilo-exporter
  namespace: monitoring
spec:
  selector:
    app: ilo-exporter
  ports:
    - name: metrics
      port: 9545
      targetPort: metrics
