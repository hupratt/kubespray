---
# Source: 03a-cert-manager.yaml, task "Install or upgrade cert-manager webhook for
# Hetzner from local path" (helm install /home/hugo/cert-manager-webhook-hetzner/chart).
# The Ansible install passed no values (chart defaults were used), so none are set
# here. Pin/adjust once the chart source above is confirmed.
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cert-manager-webhook-hetzner
  namespace: cert-manager
spec:
  releaseName: cert-manager-webhook-hetzner
  interval: 30m
  chart:
    spec:
      chart: chart
      sourceRef:
        kind: GitRepository
        name: cert-manager-webhook-hetzner
        namespace: cert-manager
      interval: 30m
  install:
    createNamespace: false
